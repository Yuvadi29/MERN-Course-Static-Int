function greet(name) {
    return `Hello, ${name}!`;
}
console.log(greet("Alice"));

// IIFE (){}

// ();

// Arrow Function () => {}

const App = (name) => {
    return `Hello ${name}`;
};
console.log(App("Aditya"));

// Create 4 functions add, subtract , multiply, divide and implement them taking your own values

// Create a function to check the eligibility of voting by taking the age as a parameter

// Create an arrow function to check if a given number is even or odd

// Create a function to print the elements of an array using loop

// Create a function to convert temperature in Celcius into Farhenheit

// Create a function to calculate Simple Interest based on principle amount, rate of interest and time

